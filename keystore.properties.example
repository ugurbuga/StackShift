# Copy this file to `keystore.properties` and fill in your local signing values.
# The real `keystore.properties` is gitignored.

keystore.storeFile=keystore/StackShift.jks
keystore.storePassword=
keystore.keyAlias=
keystore.keyPassword=

